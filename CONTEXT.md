# 领域词汇

`gamepad_mapper_qt` 的术语表。输出中提到这些概念时用这里的说法，不要漂移到「回避」列出的同义词。

## 槽位 (slot)

一个 0–23 的整数，标识手柄上的一个可映射输入。`slots.SLOTS` 的下标就是槽位号，**它是贯穿磁盘、UI、引擎的唯一标识符** —— profile JSON 的键、`MappingTable` 的行号、`InputFrame.pressed` 的下标全是它。

一个槽位的全部静态属性（名字、颜色、面板坐标、渲染形态）由一条 `Slot` 记录带齐，不再散在多张表里。

往 `SLOTS` 中间插入或重排会静默改写磁盘上所有 profile 的含义。只能追加到末尾。

- 回避：「按钮」（槽位包含 D-Pad 方向和摇杆方向，它们不是按钮）、「按键」（那是键盘侧的说法）

## 保留槽位 (reserved slot)

不走通用映射路径的槽位：6 (LT)、7 (RT)、9 (Start)、10/11 (L3/R3)。它们的**语义**写死在代码里，profile 里给它们配的映射要么不生效，要么被统一鼠标层覆盖。

## 帧 (InputFrame)

一次采样的完整结果，同时携带**边沿**和**连续量**。全应用每个 tick 只产生一帧，所有消费者读同一帧。

- 回避：「状态」「快照」——「帧」强调它是一次采样的产物且随即作废

## 边沿 (edge)

状态发生变化的那一瞬间，而非持续状态。四种：`just_pressed`、`just_released`、`just_long_pressed`、`just_short_released`。长按与短按互斥 —— 长按已触发的槽位，松开时不再算短按。

边沿判定全部集中在 `GamepadInput`。历史上它散落在两处、各自维护上一帧状态，直接导致 LT 长短按失效、Start 一半几率不响应。

- 回避：「触发」（太泛）、「事件」（会让人以为有事件队列，实际是每帧计算的集合）

## 方案 (profile)

一套针对特定前台程序的映射配置，落盘为 `config/profiles/<id>.json`，代码里是 `HarnessProfile`。

- 回避：「配置」（太泛，`app_state.json` 也是配置）、「预设」

## 闸门 (gate)

「当前前台进程是否命中本方案的 `process_names`」这个判定。闸门关闭时引擎释放全部按键、不发任何输出。`process_names` 为空列表表示**闸门恒开**（通用方案靠这个对任意窗口生效）。

- 回避：「过滤」「拦截」——闸门是开关，不筛选内容

## 可保存 (savable)

方案或应用状态是否允许写回磁盘。**从一个读不了的文件加载出来的对象是不可保存的** —— 写回去会把用户原本的内容毁掉。

关键在于守卫放在**写入侧**：`save_profile` / `save_app_state` 在碰磁盘前检查，调用方忘记检查也不会丢数据。

- 回避：「只读」（不准确 —— 对象本身可以随便改，只是不许落盘）、「无效」（文件可能完全正常，只是这一次读失败）

## 统一鼠标层 (universal mouse layer)

跨所有方案一致的那组鼠标映射（L3 左键、R3 右键）。读取时合并进方案，保存时剥离 —— 这一对互逆操作由 `ActiveProfile` 独家持有，因为「合并再剥离等于原样」这个不变式需要一个负责人。

---

术语不在表里，说明要么你在发明这个项目不用的说法，要么这里有个真实空缺 —— 后者值得补进来。
